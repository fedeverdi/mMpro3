rd_("AhAn abstract ring buffer.C`Returns reference to the underlying ring buffer.0BiThe number of items stored in the buffer.0AhLength of the container.BnThe number of items stored in the ring buffer.0C`Creates consumer from the ring buffer reference.C`Creates producer from the ring buffer reference.AmCreate new ring buffer cache.0BhCreates a new instance of a ring buffer.0ChRemoves latest item from the ring buffer and returns it.00AoReturns the argument unchanged.0000000nHead position.BaCalls <code>U::from(self)</code>.0000000CmReturns a front-to-back iterator containing references to \xe2\x80\xa600BcAppends an item to the ring buffer.00EfRemoves at most <code>n</code> and at least <code>min(n, Self::len())</code> items \xe2\x80\xa6DiRemoves exactly <code>n</code> items from the buffer and safely drops \xe2\x80\xa60BiSynchronize changes with the ring buffer.000CfCommit changes and fetch updates from the ring buffer.0nTail position.BiAn abstract reference to the ring buffer.ChRemoves all items from the buffer and safely drops them.00BnSplits ring buffer into producer and consumer.0AkHeap-allocated ring buffer.B`Basic ring buffer functionality.AeRing buffer read end.BaJust a wrapper for a ring buffer.BbCommit changes to the ring buffer.0ChReturns part of underlying raw ring buffer memory as \xe2\x80\xa6BgRing buffer for using in single thread.AfRing buffer write end.ClMoves <code>head</code> target by <code>count</code> places.CmMoves <code>tail</code> counter by <code>count</code> places.CfDon\xe2\x80\x99t publish and drop items inserted since last \xe2\x80\xa60BiDiscard new items pushed since last sync.BbChecks if the ring buffer is full.0BdChecks if the vacant range is empty.0CkModulus for <code>head</code> and <code>tail</code> values.0DfCommit and destroy <code>Self</code> returning underlying ring buffer.0CiLock-free SPSC FIFO ring buffer with direct access to \xe2\x80\xa6CnCreates a new instance of a ring buffer returning an error \xe2\x80\xa6AmConsumer part of ring buffer.BiInternal representation of the container.AmProducer part of ring buffer.CaRing buffer that could be shared between threads.CaStack-allocated ring buffer with static capacity.BdReturns capacity of the ring buffer.000Bk<code>Consumer</code> and additional types.CbThe number of remaining free places in the buffer.000BcChecks if the ring buffer is empty.0BfChecks if the occupied range is empty.0CiReturns a front-to-back iterator that returns mutable \xe2\x80\xa600CnReturns an iterator that removes items one by one from the \xe2\x80\xa600Bk<code>Producer</code> and additional types.BlSets the new <strong>head</strong> position.BlSets the new <strong>tail</strong> position.FeMoves at most <code>count</code> items from the <code>src</code> consumer to the <code>dst</code> \xe2\x80\xa6BgAbstract container for the ring buffer.CiReturns a pair of slices which contain, in order, the \xe2\x80\xa600ClRemoves first items from the ring buffer and writes them \xe2\x80\xa600CjReturns postponed consumer that borrows <code>Self</code>.CjReturns postponed producer that borrows <code>Self</code>.CfAppends items from an iterator to the ring buffer. \xe2\x80\xa600EfReads at most <code>count</code> bytes from <code>Read</code> instance and appends \xe2\x80\xa6CiSplits ring buffer into producer and consumer without \xe2\x80\xa60CgReturn pointer to the beginning of the container items.CnAppends items from slice to the ring buffer. Elements must \xe2\x80\xa600ChThe number of vacant places in the buffer at the moment.0DkRemoves at most first <code>count</code> bytes from the ring buffer and \xe2\x80\xa6CdAn iterator that removes items from the ring buffer.BeCaching read end of some ring buffer.DhConsumes <code>self</code> and returns underlying ring buffer reference.0BgRing buffer traits and implementations.CdAlias for <code>HeapRb</code> <code>Consumer</code>.CdAlias for <code>HeapRb</code> <code>Producer</code>.BfCaching write end of some ring buffer.DhMove <strong>head</strong> position by <code>count</code> items forward.0DhMove <strong>tail</strong> position by <code>count</code> items forward.0CgThe number of items stored in the buffer at the moment.0CmReturns a pair of mutable slices which contain, in order, \xe2\x80\xa600BoRestore container from internal representation.BoTransform container to internal representation.AnCreate new postponed consumer.0AnCreate new postponed producer.0CjRemoves items from the head of ring buffer and drops them.0DiReturns a pair of ranges of <code>Self::vacant_slices</code> location \xe2\x80\xa60CnProvides a direct access to the ring buffer vacant memory. \xe2\x80\xa60CfAlias for <code>StaticRb</code> <code>Consumer</code>.CfAlias for <code>StaticRb</code> <code>Producer</code>.CcConstructs ring buffer from container and counters.0CeSynchronize and transform back to immediate consumer.0CeSynchronize and transform back to immediate producer.0CeTransforms <code>Self</code> into postponed consumer.CeTransforms <code>Self</code> into postponed producer.DkDestructures ring buffer into underlying container and <code>head</code>\xe2\x80\xa60ClPushes an item to the ring buffer overwriting the latest \xe2\x80\xa60DkReturns a pair of ranges of <code>Self::occupied_slices</code> location \xe2\x80\xa60CgProvides a direct mutable access to the ring buffer \xe2\x80\xa60ChProvides a direct access to the ring buffer occupied \xe2\x80\xa6AlCapacity of the ring buffer.AcPostponed consumer.AcPostponed producer.CbAppends items from an iterator to the ring buffer.05CnProvides a direct access to the ring buffer vacant memory. \xe2\x80\xa6CkAppends items from slice to the ring buffer overwriting \xe2\x80\xa60")